{
  "name": "zod-form-kit-monorepo",
  "version": "1.0.0",
  "description": "Monorepo for zod-form-kit with core library, website, and UI integrations",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter website dev",
    "build": "pnpm --recursive build",
    "test": "pnpm --recursive test",
    "lint": "pnpm --recursive lint",
    "lint:fix": "pnpm --recursive lint:fix",
    "format": "pnpm --recursive format",
    "clean": "pnpm --recursive clean",
    "ladle": "pnpm --filter core ladle",
    "ladle:build": "pnpm --filter core ladle:build"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.4.0",
    "typescript": "^5.2.0"
  },
  "keywords": [
    "zod",
    "forms",
    "typescript",
    "validation",
    "form-generator",
    "ui-agnostic",
    "adapter-pattern",
    "schema-driven",
    "framework-agnostic",
    "monorepo",
    "radix-ui",
    "shadcn",
    "zod-form-kit"
  ],
  "author": "Your Name",
  "license": "MIT"
}